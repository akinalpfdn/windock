import SwiftUI

// Placeholder - WinDock runs as a background agent with no main window.
// Preview is shown via PreviewPanel when hovering macOS dock icons.
struct ContentView: View {
    var body: some View {
        EmptyView()
    }
}
